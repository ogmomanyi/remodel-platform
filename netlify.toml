[build]
  command = "cd remodel-app && npm run build"
  publish = "remodel-app/.next"
  base = "remodel-app"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  node_bundler = "esbuild"
